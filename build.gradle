subprojects {
    buildscript {
        repositories {
            jcenter()
            google()
        }
    }
    repositories {
        jcenter()
        google()
    }
}

apply from: rootProject.file("gradle/dependencies.gradle")